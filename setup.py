import os
from setuptools import setup, find_packages

setup(
    name = "twitter_storm",
    version = "0.0.1",
    description = ("Loggi twitter storm hiring challenge"),
    packages=find_packages()
)
